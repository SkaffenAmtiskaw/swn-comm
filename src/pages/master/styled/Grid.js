import styled from 'styled-components';

const Grid = styled.div`
  margin-top: 8px;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-column-gap: 8px;
  grid-row-gap: 8px;
`;

export  default Grid;
